# Ekklesia — AI CHATBOT  ·  STATUS: NOT BUILT
There is no AI chatbot code. It has not been built. This note exists so the handoff
is honest. Proposed scope: a natural-language "find a church / pastor / event"
assistant over the Ekklesia database (chat UI + API route + query/embeddings).
Say the word and it can be built for real.
